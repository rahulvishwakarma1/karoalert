name: Android APK Build

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        working-directory: ./karoalertfrontendapp

      - name: Expo Prebuild
        run: npx expo prebuild --platform android --clean
        working-directory: ./karoalertfrontendapp

      - name: Build Release APK
        run: ./gradlew assembleRelease
        working-directory: ./karoalertfrontendapp/android

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: karoalertfrontendapp/android/app/build/outputs/apk/release/*.apk
          retention-days: 7
